# SPA fallback (Cloudflare Pages and Netlify both read this file): every path is
# served by index.html so client-side routes like /dashboard or /applications/:id
# survive a hard refresh.
/*  /index.html  200
